<Application x:Class="HomeTechServices.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:HomeTechServices"
             StartupUri="Login.xaml">
    <Application.Resources>

        <DataTemplate x:Key="ProductTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Service_Name}" Width="400" FontFamily="Arial" FontSize="14" FontWeight="Bold"/>
                <TextBlock Text="{Binding Service_Cost}" Width="100" TextWrapping="Wrap"/>
            </StackPanel>
        </DataTemplate>

        <DataTemplate x:Key="TechTemplate">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="{Binding Name}" Width="200" FontFamily="Arial" FontSize="14" FontWeight="Bold"/>
                <TextBlock Text="{Binding PhoneNumber}" Width="150" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Email}" Width="150" TextWrapping="Wrap"/>
            </StackPanel>
        </DataTemplate>

    </Application.Resources>
</Application>
